import styled from "styled-components";

const Wrapper = styled.div`
`

const SummonerChampion = () => {
    return(
        <Wrapper>
            <p>Champion</p>
        </Wrapper>
    );
};

export default SummonerChampion;