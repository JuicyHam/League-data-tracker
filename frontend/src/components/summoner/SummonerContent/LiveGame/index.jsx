import styled from "styled-components";

const Wrapper = styled.div`
`

const LiveGame = () => {
    return(
        <Wrapper>
            <p>Live Game</p>
        </Wrapper>
    );
};

export default LiveGame;